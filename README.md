# Reaction Capture

Prototype for testing LLM analysis of body language and micro-expressions captured during reaction image viewing.

## Setup

```bash
uv sync
cp .env.example .env
# Edit .env with your GEMINI_API_KEY
```

## Usage

```bash
uv run python -m capture_service.main
```

Press `q` to quit.
